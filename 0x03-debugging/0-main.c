#include "main.h"

/**
 * main - Test positive or negative function defined in 
 *	  main.h
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num = 0;

	positive_or_negative(i);
	return (0);
}
