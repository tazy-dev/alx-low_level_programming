#include "main.h"

/**
 * _abs - Calculate the absolute value.
 * @num: The num to be tested.
 *
 * Return: The absolute value of the integer.
 */

int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
