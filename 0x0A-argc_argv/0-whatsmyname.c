#include "main.h"

/**
 * main - Prints the name of tthe program.
 * @argc: Arguments Count.
 * @argv: Arguments Vector
 *
 * Return: Return 0 (Success).
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
